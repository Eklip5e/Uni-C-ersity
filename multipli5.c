#include <stdlib.h>
#include <stdio.h>

int main()
{
    int n, i, multi;
    i=0;
    n=40;
    multi=5;

    for (i=1; i<=n; i++)
    {
        multi=5*(i);
        if (i==1)
            printf("\nIl primo multiplo di 5 e\': %d\n", multi);
        else if ()
        printf("\nIl %d multiplo di 5 e\': %d\n", i, multi);
    }

    return 0;
}
